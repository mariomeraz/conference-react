import React from 'react';
import SponsorsList from './SponsorsList';

const SponsorsIndex = () => {
  return (
      <div>
        <SponsorsList />
      </div>
  );
};

export default SponsorsIndex;
